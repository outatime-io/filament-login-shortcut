# Contributing

Please discuss security-sensitive changes before opening a pull request. Run `composer validate`, `composer format`, `composer analyse`, and `composer test`. Keep changes focused, add behavioural Pest coverage, and never add a bypass that weakens the enablement, authorization, or panel-access checks.
